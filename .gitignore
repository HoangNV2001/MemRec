# Secrets
.env
.env.*
!.env.example
secrets/
# Cluster access/runbook contains internal host/account/job details. Keep it
# local like the source project's internal_docs policy.
internal_docs/

# Data (large, not tracked)
data/iagent/
data/amazon_books/raw/*
data/processed/*
!data/processed/.gitkeep
# Amazon Books artifacts are fully derived. Keep a directory marker only.
data/amazon_books/artifacts/*
!data/amazon_books/artifacts/.gitkeep
# MovieLens 32M official files are local research data. Derived artifacts are
# reproducible and remain local; retain only the directory marker in git.
data/ml-32m/*.csv
data/ml-32m/README.txt
data/ml-32m/checksums.txt
data/ml-32m/artifacts/*
!data/ml-32m/artifacts/.gitkeep

# Results & checkpoints
results/
checkpoints/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# OS / editor
.DS_Store
.vscode/
.idea/

# Logs
*.log
