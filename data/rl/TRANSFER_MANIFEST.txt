MemRec RL data bundles — what these are and where they go
=========================================================
Generated 2026-08-07 from branch rl/m2-reward (M1 + M2 Part A + M2 Part B complete).
Full context: docs/DEPLOY_GPU.md · results: docs/RESULTS.md "M2 Reward Validation"

BOTH TARBALLS STORE PATHS RELATIVE TO THE REPO ROOT.
Extract at the repo root and everything lands in place — nothing to move:

    cd /path/to/MemRec
    tar xzf memrec_rl_data.tgz
    tar xzf memrec_rl_extras.tgz        # optional, see below
    python -m src.rl.verify_transfer    # confirms it landed correctly

>>> CHANGED AT M2 PART B (2026-08-07) — a pre-2026-08-07 bundle is now STALE <<<
The three jsonl files now carry back-filled `r_null`, `baseline_h1` and
`baseline_p_gold`, produced by Qwen2.5-3B-Instruct in float32. Line counts are
unchanged (1185/149/993) but the sha256s are different, and the expected hashes
in src/rl/verify_transfer.py were updated to match. Restoring an older bundle on
top of current code will fail verification — re-pack instead of reusing.

----------------------------------------------------------------------
memrec_rl_data.tgz  — REQUIRED
----------------------------------------------------------------------
  data/rl/stager_books_train.jsonl      14 MB   1185 records   M3 SFT, M4 GRPO
  data/rl/stager_books_test.jsonl       12 MB    993 records   final eval
  data/rl/stager_books_val.jsonl       1.7 MB    149 records   M2-B, eval during training
  data/rl/m2_val_reference_books.json  440 KB   cached gpt-4o-mini half of Validation A/B
                                                (~$0.5 + 6 min of API to regenerate)
  data/rl/user_splits_books.json        28 KB   also tracked in git; defines the partition
  data/rl/baselines_provenance.json     <1 KB   WHICH ranker produced the back-filled
                                                fields, per split. Three fields in the
                                                jsonl are properties of one specific
                                                ranker and are indistinguishable by eye;
                                                this is how a wrong-model or half-finished
                                                back-fill is caught.
  data/rl/m2_validation_report*.json    ~6 KB   M2 Part B evidence (1.5B, 3B, 3B-fp32,
  data/rl/m2_pairs*.json               ~450 KB  no-instruction) + per-(user,arm) scores.
                                                Small, and re-earning them costs GPU time.

----------------------------------------------------------------------
memrec_rl_extras.tgz  — OPTIONAL but expensive to re-earn
----------------------------------------------------------------------
  data/rl/graph_snapshot_books.json     17 MB   rebuild the jsonl offline if the prompt
                                                template changes, without data/processed/
  results/m1_warmup_2350/memory_warmup_only.json
                                        52 MB   the ONE costly M1 artifact (~$3.15 of API).
                                                Losing it means paying that again. Keep a
                                                copy somewhere durable, not just on a
                                                rented box.

----------------------------------------------------------------------
NOT in these bundles (and not in git either)
----------------------------------------------------------------------
  .env                    — copy separately; needed for gpt-4o-mini at M3/M7a, not for M2-B
  data/processed/         — 1.1 GB, only needed to REBUILD the dataset
  HF model weights        — re-download on the new box:
                              Qwen/Qwen2.5-3B-Instruct   ~6.2 GB  reward ranker (M2-B)
                              BAAI/bge-small-en-v1.5     ~130 MB  grounding, runs on CPU
